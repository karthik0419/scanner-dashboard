Optimize Docker images: 3.0GB → 1.79GB (40% reduction)

Frontend (709MB → 224MB, 68% smaller):
- Enable Next.js standalone output mode
- Ship only the minimal server + static assets (no node_modules)
- Multi-stage Dockerfile (builder has all deps, runtime is tiny)
- CMD ["node", "server.js"] instead of npm start

Backend + Worker (1.15GB → 784MB each, 32% smaller):
- Multi-stage Dockerfile: build stage has gcc/g++/dev headers, runtime stage only has runtime libs
- Use Python venv to copy only installed packages (no build tools in final image)
- Remove gcc, g++, libpq-dev, libxml2-dev, libxslt1-dev from runtime
- Add only runtime shared libs (libpq5, libxml2, libxslt1.1)
- Set PYTHONUNBUFFERED=1, PYTHONDONTWRITEBYTECODE=1

Docker Compose improvements:
- Health checks on all 5 containers (backend, worker, frontend, postgres, redis)
- Memory limits: postgres 512M, redis 128M, backend 512M, worker 1G, frontend 256M
- Frontend depends_on backend with condition: service_healthy
- restart: unless-stopped on postgres + redis (was missing)

Verified: scan completes with 10 picks, kill works, worker survives, all health checks green.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
